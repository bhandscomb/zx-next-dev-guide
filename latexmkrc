$pdf_mode = 1;                            # e.g. 1=pdflatex 5=xelatex
$aux_dir = "temp";                        # try to keep source dir clean
$out_dir = "build";                       # where to put final output
@default_files = ('zx-next-dev-guide');   # master document
